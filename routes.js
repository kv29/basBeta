var express = require('express')
var config = require(__dirname + '/config')
var appDir = config.appDir
var router = express.Router()

//router.post('/api/v1/teamCalendar',teamCalendar.getTeamCalendar)

module.exports = router

